language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
  - "3.9"

install:
  - pip install pipenv
  - pipenv install --dev

before_script:
  - export PYTHONPATH=$PYTHONPATH:$(pwd)

script:
  - pipenv run pytest -v --cov=fabular --cov-report=xml
#  - pipenv run bandit fabular -r
